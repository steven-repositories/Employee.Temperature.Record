namespace Employee.Temperature.Record.Api.Enums {
    public enum AuthTypes {
        New,
        Optimization,
        Filter
    }

    public enum ManageTypes {
        Pull,
        Delete
    }

    public enum PullTypes {
        Single,
        All
    }
}
